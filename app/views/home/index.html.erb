<h1 class="text-center">Todos los anuncios</h1>
<% if account_signed_in? %>
  <% if !current_account.first_name? %>
    <p class="alert-box callout success">Edita tu perfil para que aparezca tu
    nombre no tu correo, si lo quires <%= link_to 'Edit profile', edit_account_registration_path, :class => 'navbar-link' %> </p>
  <% end %>
<% end %>

<% (1..3).each do |single| %>
<div class="callout secondary">
  <h5>This is a secondary callout <%= single %></h5>
  <p>It has an easy to override visual style, and is appropriately subdued.</p>
  <a href="#">It's dangerous to go alone, take this.</a>
</div>
<% end %>