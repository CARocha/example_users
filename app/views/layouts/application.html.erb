<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Anuncios-examen" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="grid-container">
        <%= render 'layouts/navbar' %>
        <% if notice %>
          <p class="alert-box callout success"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="alert-box callout alert"><%= alert %></p>
        <% end %>
        <%= yield %>
        <%= render 'layouts/footer' %>
    </div>
  </body>
</html>